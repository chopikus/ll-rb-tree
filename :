#include <iostream>
#include <vector>
#include <algorithm>
#include "set.h"

using namespace std;

int main() {
    Set<pair<int, int> > my_set = {{1, 2}};
    Set<pair<int, int> > my_set2 = my_set;
    auto it = ++my_set.begin();
    cout << (it)->first;
    return 0;
}
